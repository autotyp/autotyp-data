# ------ VInflAktionsart.Position.binned5 ------
VInflAktionsart.Position.binned5:
  Description : >
     Position of separate markers of "Aktionsart' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflAktionsart.Position.binned5'
  N.levels : 3
  N.entries : 33
  N.languages : 32
  N.missing : 230
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Aktionsart", see the metadata for VInflCat in Synthesis

# ------ VInflApplicative.Position.binned5 ------
VInflApplicative.Position.binned5:
  Description : >
     Position of separate markers of "Applicative' (cf. Bickel & Nichols 2005 Inflectional Synthesis
     of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflApplicative.Position.binned5'
  N.levels : 2
  N.entries : 23
  N.languages : 22
  N.missing : 240
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Applicative", see the metadata for VInflCat in Synthesis

# ------ VInflAspect.Position.binned5 ------
VInflAspect.Position.binned5:
  Description : >
     Position of separate markers of "Aspect' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflAspect.Position.binned5'
  N.levels : 5
  N.entries : 99
  N.languages : 94
  N.missing : 164
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
    'simul' : >
       [the formative has two exponents at different positions in the phonological host (but it is not
       the case that one is positioned before and the other after the host, which is coded as “circum”)],
       [the formative has two exponents, one before and one after the phonological host. This is a special
       case of the “simul” position], [around the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [around the phonological host by default, after the
       host under exceptional conditions (typically lexical)], [in two or more different positions in
       (but not around) the phonological host by default, before the host under exceptional conditions
       (typically lexical)], [in two or more different positons in the phonological host by default,
       after the host under exceptional conditions (typically lexical)], [a special case of “simul” where
       one position is stem change itself]
  Notes : >
     For the definition of "Aspect", see the metadata for VInflCat in Synthesis

# ------ VInflCausative.Position.binned5 ------
VInflCausative.Position.binned5:
  Description : >
     Position of separate markers of "Causative' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflCausative.Position.binned5'
  N.levels : 4
  N.entries : 43
  N.languages : 42
  N.missing : 220
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
  Notes : >
     For the definition of "Causative", see the metadata for VInflCat in Synthesis

# ------ VInflClassifier.Position.binned5 ------
VInflClassifier.Position.binned5:
  Description : >
     Position of separate markers of "Classifier' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflClassifier.Position.binned5'
  N.levels : 1
  N.entries : 2
  N.languages : 2
  N.missing : 261
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Classifier", see the metadata for VInflCat in Synthesis

# ------ VInflConnective.Position.binned5 ------
VInflConnective.Position.binned5:
  Description : >
     Position of separate markers of "Connective' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflConnective.Position.binned5'
  N.levels : 2
  N.entries : 10
  N.languages : 10
  N.missing : 253
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Connective", see the metadata for VInflCat in Synthesis

# ------ VInflConstruct.Position.binned5 ------
VInflConstruct.Position.binned5:
  Description : >
     Position of separate markers of "Construct' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflConstruct.Position.binned5'
  N.levels : 2
  N.entries : 2
  N.languages : 2
  N.missing : 261
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Construct", see the metadata for VInflCat in Synthesis

# ------ VInflControl.Position.binned5 ------
VInflControl.Position.binned5:
  Description : >
     Position of separate markers of "Control' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflControl.Position.binned5'
  N.levels : 1
  N.entries : 2
  N.languages : 2
  N.missing : 261
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Control", see the metadata for VInflCat in Synthesis

# ------ VInflDeixis.Position.binned5 ------
VInflDeixis.Position.binned5:
  Description : >
     Position of separate markers of "Deixis' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflDeixis.Position.binned5'
  N.levels : 2
  N.entries : 35
  N.languages : 35
  N.missing : 228
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Deixis", see the metadata for VInflCat in Synthesis

# ------ VInflVoice.Position.binned5 ------
VInflVoice.Position.binned5:
  Description : >
     Position of separate markers of "Voice' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflVoice.Position.binned5'
  N.levels : 4
  N.entries : 59
  N.languages : 58
  N.missing : 204
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Diathesis", see the metadata for VInflCat in Synthesis

# ------ VInflFocus.Position.binned5 ------
VInflFocus.Position.binned5:
  Description : >
     Position of separate markers of "Focus' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflFocus.Position.binned5'
  N.levels : 2
  N.entries : 8
  N.languages : 7
  N.missing : 255
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Focus", see the metadata for VInflCat in Synthesis

# ------ VInflPosterior.Position.binned5 ------
VInflPosterior.Position.binned5:
  Description : >
     Position of separate markers of "Posterior' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflPosterior.Position.binned5'
  N.levels : 2
  N.entries : 5
  N.languages : 5
  N.missing : 258
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Future_Posterior", see the metadata for VInflCat in Synthesis

# ------ VInflGender.Position.binned5 ------
VInflGender.Position.binned5:
  Description : >
     Position of separate markers of "Gender' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflGender.Position.binned5'
  N.levels : 2
  N.entries : 4
  N.languages : 4
  N.missing : 259
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Gender", see the metadata for VInflCat in Synthesis

# ------ VInflIllocution.Position.binned5 ------
VInflIllocution.Position.binned5:
  Description : >
     Position of separate markers of "Illocution' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflIllocution.Position.binned5'
  N.levels : 2
  N.entries : 21
  N.languages : 20
  N.missing : 242
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Illocution", see the metadata for VInflCat in Synthesis

# ------ VInflMA.Position.binned5 ------
VInflMA.Position.binned5:
  Description : >
     Position of separate markers of "MA' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the Verb,
     in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflMA.Position.binned5'
  N.levels : 2
  N.entries : 3
  N.languages : 3
  N.missing : 260
  Levels : 
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "MA", see the metadata for VInflCat in Synthesis

# ------ VInflMN.Position.binned5 ------
VInflMN.Position.binned5:
  Description : >
     Position of separate markers of "MN' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the Verb,
     in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflMN.Position.binned5'
  N.levels : 2
  N.entries : 2
  N.languages : 2
  N.missing : 261
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "MN", see the metadata for VInflCat in Synthesis

# ------ VInflModality.Position.binned5 ------
VInflModality.Position.binned5:
  Description : >
     Position of separate markers of "Modality' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflModality.Position.binned5'
  N.levels : 2
  N.entries : 10
  N.languages : 10
  N.missing : 253
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Modality", see the metadata for VInflCat in Synthesis

# ------ VInflMood.Position.binned5 ------
VInflMood.Position.binned5:
  Description : >
     Position of separate markers of "Mood' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflMood.Position.binned5'
  N.levels : 3
  N.entries : 53
  N.languages : 53
  N.missing : 210
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
  Notes : >
     For the definition of "Mood", see the metadata for VInflCat in Synthesis

# ------ VInflMotion.Position.binned5 ------
VInflMotion.Position.binned5:
  Description : >
     Position of separate markers of "Motion' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflMotion.Position.binned5'
  N.levels : 1
  N.entries : 7
  N.languages : 7
  N.missing : 256
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Motion", see the metadata for VInflCat in Synthesis

# ------ VInflMSE.Position.binned5 ------
VInflMSE.Position.binned5:
  Description : >
     Position of separate markers of "MSE' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the Verb,
     in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflMSE.Position.binned5'
  N.levels : 2
  N.entries : 4
  N.languages : 4
  N.missing : 259
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "MSE", see the metadata for VInflCat in Synthesis

# ------ VInflNominalizer.Position.binned5 ------
VInflNominalizer.Position.binned5:
  Description : >
     Position of separate markers of "Nominalizer' (cf. Bickel & Nichols 2005 Inflectional Synthesis
     of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflNominalizer.Position.binned5'
  N.levels : 1
  N.entries : 3
  N.languages : 3
  N.missing : 260
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Nominalizer", see the metadata for VInflCat in Synthesis

# ------ VInflNumber.Position.binned5 ------
VInflNumber.Position.binned5:
  Description : >
     Position of separate markers of "Number' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflNumber.Position.binned5'
  N.levels : 4
  N.entries : 52
  N.languages : 49
  N.missing : 211
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Number", see the metadata for VInflCat in Synthesis

# ------ VInflPerson.Position.binned5 ------
VInflPerson.Position.binned5:
  Description : >
     Position of separate markers of "Person' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflPerson.Position.binned5'
  N.levels : 2
  N.entries : 6
  N.languages : 6
  N.missing : 257
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Person", see the metadata for VInflCat in Synthesis

# ------ VInflPolarity.Position.binned5 ------
VInflPolarity.Position.binned5:
  Description : >
     Position of separate markers of "Polarity' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflPolarity.Position.binned5'
  N.levels : 5
  N.entries : 90
  N.languages : 90
  N.missing : 173
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'simul' : >
       [the formative has two exponents at different positions in the phonological host (but it is not
       the case that one is positioned before and the other after the host, which is coded as “circum”)],
       [the formative has two exponents, one before and one after the phonological host. This is a special
       case of the “simul” position], [around the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [around the phonological host by default, after the
       host under exceptional conditions (typically lexical)], [in two or more different positions in
       (but not around) the phonological host by default, before the host under exceptional conditions
       (typically lexical)], [in two or more different positons in the phonological host by default,
       after the host under exceptional conditions (typically lexical)], [a special case of “simul” where
       one position is stem change itself]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Polarity", see the metadata for VInflCat in Synthesis

# ------ VInflPotentialis.Position.binned5 ------
VInflPotentialis.Position.binned5:
  Description : >
     Position of separate markers of "Potentialis' (cf. Bickel & Nichols 2005 Inflectional Synthesis
     of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflPotentialis.Position.binned5'
  N.levels : 1
  N.entries : 1
  N.languages : 1
  N.missing : 262
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Potentialis", see the metadata for VInflCat in Synthesis

# ------ VInflQuantificational.Position.binned5 ------
VInflQuantificational.Position.binned5:
  Description : >
     Position of separate markers of "Quantificational' (cf. Bickel & Nichols 2005 Inflectional Synthesis
     of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflQuantificational.Position.binned5'
  N.levels : 4
  N.entries : 50
  N.languages : 48
  N.missing : 213
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Quantificational", see the metadata for VInflCat in Synthesis

# ------ VInflReflexive_and_Reciprocal.Position.binned5 ------
VInflReflexive_and_Reciprocal.Position.binned5:
  Description : >
     Position of separate markers of "Reflexive_and_Reciprocal' (cf. Bickel & Nichols 2005 Inflectional
     Synthesis of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflReflexive_and_Reciprocal.Position.binned5'
  N.levels : 3
  N.entries : 35
  N.languages : 35
  N.missing : 228
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
  Notes : >
     For the definition of "Reflexive_and_Reciprocal", see the metadata for VInflCat in Synthesis

# ------ VInflRECIP.Position.binned5 ------
VInflRECIP.Position.binned5:
  Description : >
     Position of separate markers of "RECIP' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflRECIP.Position.binned5'
  N.levels : 3
  N.entries : 10
  N.languages : 10
  N.missing : 253
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "RECIP", see the metadata for VInflCat in Synthesis

# ------ VInflReferential.Position.binned5 ------
VInflReferential.Position.binned5:
  Description : >
     Position of separate markers of "Referential' (cf. Bickel & Nichols 2005 Inflectional Synthesis
     of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflReferential.Position.binned5'
  N.levels : 2
  N.entries : 7
  N.languages : 7
  N.missing : 256
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Referential", see the metadata for VInflCat in Synthesis

# ------ VInflReflexive.Position.binned5 ------
VInflReflexive.Position.binned5:
  Description : >
     Position of separate markers of "Reflexive' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflReflexive.Position.binned5'
  N.levels : 3
  N.entries : 14
  N.languages : 14
  N.missing : 249
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Reflexive", see the metadata for VInflCat in Synthesis

# ------ VInflRepetition.Position.binned5 ------
VInflRepetition.Position.binned5:
  Description : >
     Position of separate markers of "Repetition' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflRepetition.Position.binned5'
  N.levels : 3
  N.entries : 4
  N.languages : 4
  N.missing : 259
  Levels : 
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Repetition", see the metadata for VInflCat in Synthesis

# ------ VInflReversative.Position.binned5 ------
VInflReversative.Position.binned5:
  Description : >
     Position of separate markers of "Reversative' (cf. Bickel & Nichols 2005 Inflectional Synthesis
     of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflReversative.Position.binned5'
  N.levels : 1
  N.entries : 1
  N.languages : 1
  N.missing : 262
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Reversative", see the metadata for VInflCat in Synthesis

# ------ VInflSemistem.Position.binned5 ------
VInflSemistem.Position.binned5:
  Description : >
     Position of separate markers of "Semistem' (cf. Bickel & Nichols 2005 Inflectional Synthesis of
     the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflSemistem.Position.binned5'
  N.levels : 1
  N.entries : 10
  N.languages : 10
  N.missing : 253
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Semistem", see the metadata for VInflCat in Synthesis

# ------ VInflSpatial.Position.binned5 ------
VInflSpatial.Position.binned5:
  Description : >
     Position of separate markers of "Spatial' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflSpatial.Position.binned5'
  N.levels : 2
  N.entries : 5
  N.languages : 5
  N.missing : 258
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Spatial", see the metadata for VInflCat in Synthesis

# ------ VInflStatus.Position.binned5 ------
VInflStatus.Position.binned5:
  Description : >
     Position of separate markers of "Status' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflStatus.Position.binned5'
  N.levels : 4
  N.entries : 23
  N.languages : 22
  N.missing : 240
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'simul' : >
       [the formative has two exponents at different positions in the phonological host (but it is not
       the case that one is positioned before and the other after the host, which is coded as “circum”)],
       [the formative has two exponents, one before and one after the phonological host. This is a special
       case of the “simul” position], [around the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [around the phonological host by default, after the
       host under exceptional conditions (typically lexical)], [in two or more different positions in
       (but not around) the phonological host by default, before the host under exceptional conditions
       (typically lexical)], [in two or more different positons in the phonological host by default,
       after the host under exceptional conditions (typically lexical)], [a special case of “simul” where
       one position is stem change itself]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Status", see the metadata for VInflCat in Synthesis

# ------ VInflSwitch_reference.Position.binned5 ------
VInflSwitch_reference.Position.binned5:
  Description : >
     Position of separate markers of "Switch_reference' (cf. Bickel & Nichols 2005 Inflectional Synthesis
     of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflSwitch_reference.Position.binned5'
  N.levels : 2
  N.entries : 5
  N.languages : 5
  N.missing : 258
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
  Notes : >
     For the definition of "Switch_reference", see the metadata for VInflCat in Synthesis

# ------ VInflTense_and_Evidential.Position.binned5 ------
VInflTense_and_Evidential.Position.binned5:
  Description : >
     Position of separate markers of "Tense_and_Evidential' (cf. Bickel & Nichols 2005 Inflectional Synthesis
     of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflTense_and_Evidential.Position.binned5'
  N.levels : 2
  N.entries : 5
  N.languages : 5
  N.missing : 258
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
  Notes : >
     For the definition of "Tense_and_Evidential", see the metadata for VInflCat in Synthesis

# ------ VInflTA.Position.binned5 ------
VInflTA.Position.binned5:
  Description : >
     Position of separate markers of "TA' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the Verb,
     in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflTA.Position.binned5'
  N.levels : 4
  N.entries : 28
  N.languages : 24
  N.missing : 235
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "TA", see the metadata for VInflCat in Synthesis

# ------ VInflTAM.Position.binned5 ------
VInflTAM.Position.binned5:
  Description : >
     Position of separate markers of "TAM' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the Verb,
     in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflTAM.Position.binned5'
  N.levels : 4
  N.entries : 93
  N.languages : 86
  N.missing : 170
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "TAM", see the metadata for VInflCat in Synthesis

# ------ VInflTense.Position.binned5 ------
VInflTense.Position.binned5:
  Description : >
     Position of separate markers of "Tense' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflTense.Position.binned5'
  N.levels : 3
  N.entries : 80
  N.languages : 77
  N.missing : 183
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'split' : >
       [either before or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either inside or before the phonological host, conditioned by some structural
       context (category, syntax etc.)], [either inside or after the phonological host, conditioned by
       some structural context (category, syntax etc.)], [either before or around the phonological host,
       conditioned by some structural context (category, syntax etc.)], [either before, after or around
       the phonological host, conditioned by some structural context (category, syntax etc.)], [either
       before, inside or after the phonological host, conditioned by some structural context (category,
       syntax etc.)], [either before, after or in two or more positions in the phonological host, conditioned
       by some structural context (category, syntax etc.)], [either before, after, inside or around the
       phonological host, conditioned by some structural context (category, syntax etc.)], [either after
       or around the phonological host, conditioned by some structural context (category, syntax etc.)],
       [either before or inside the phonological host, conditioned by some structural context (category,
       syntax etc.)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Tense", see the metadata for VInflCat in Synthesis

# ------ VInflTM.Position.binned5 ------
VInflTM.Position.binned5:
  Description : >
     Position of separate markers of "TM' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the Verb,
     in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflTM.Position.binned5'
  N.levels : 2
  N.entries : 23
  N.languages : 23
  N.missing : 240
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "TM", see the metadata for VInflCat in Synthesis

# ------ VInflValence.Position.binned5 ------
VInflValence.Position.binned5:
  Description : >
     Position of separate markers of inflectional category "Valence' (cf. Bickel & Nichols 2005 Inflectional
     Synthesis of the Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflValence.Position.binned5'
  N.levels : 3
  N.entries : 7
  N.languages : 7
  N.missing : 256
  Levels : 
    'post' : >
       [after the phonological host], [after the phonological host by default, before the host under
       exceptional conditions (typically lexical)], [after the phonological host by default, around the
       host under exceptional conditions (typically lexical)], [after the phonological host by default,
       inside the host under exceptional conditions (typically lexical)]
    'in' : >
       [inside the phonological host], [inside the phonological host by default, after the host under
       exceptional conditions (typically lexical)]
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Valence", see the metadata for VInflCat in Synthesis

# ------ VInflVersion.Position.binned5 ------
VInflVersion.Position.binned5:
  Description : >
     Position of separate markers of "Version' (cf. Bickel & Nichols 2005 Inflectional Synthesis of the
     Verb, in WALS)
  SetUp : 'single aggregated entry per language'
  DataEntry : 'derived'
  VariableType : 'data'
  DataType : 'categorical'
  VariantOf : 'VInflVersion.Position.binned5'
  N.levels : 1
  N.entries : 3
  N.languages : 3
  N.missing : 260
  Levels : 
    'prae' : >
       [before the phonological host], [before the phonological host by default, around the host under
       exceptional conditions (typically lexical)], [before the phonological host by default, after the
       host under exceptional conditions (typically lexical)]
  Notes : >
     For the definition of "Version", see the metadata for VInflCat in Synthesis

